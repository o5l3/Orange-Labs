# 이 파일을 .env 로 복사해 값을 채운다. .env 자체는 저장소에 올리지 않는다.
# 배포 환경 값은 Vercel 프로젝트 설정 > Environment Variables 에 등록한다.

# 현재 코드에서 쓰는 곳은 없다. 문의 폼(api/send-mail.ts)을 되살릴 때 필요하다.
# SMTP_USER=     발신 Gmail 주소
# SMTP_PASS=     Gmail 앱 비밀번호 (16자리, 공백 없이)
# CONTACT_EMAIL= 문의 메일 수신 주소
